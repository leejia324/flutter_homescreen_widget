name: flutter_homescreen_widget
description: >-
  Update iOS WidgetKit and Android Glance widgets using Flutter as the UI.
  Pure Dart — no Swift or Kotlin required. Supports tap actions and multiple
  sizes.
version: 0.1.3
homepage: https://github.com/leejia324/flutter_homescreen_widget
repository: https://github.com/leejia324/flutter_homescreen_widget
issue_tracker: https://github.com/leejia324/flutter_homescreen_widget/issues

environment:
  sdk: ^3.11.4
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.leejia.flutter_homescreen_widget
        pluginClass: FlutterHomescreenWidgetPlugin
      ios:
        pluginClass: FlutterHomescreenWidgetPlugin
